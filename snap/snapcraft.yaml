name: ccomixmanager # you probably want to 'snapcraft register <name>'
base: core20 # the base snap is the execution environment for this snap
version: '0.1.2.6' # just for humans, typically '1.2+git' or '1.3.2'
summary: Il primo gestionale per snap in gtk e mariadb # 79 char long summary
description: |
  Il primo gestionale in gtk e mariadb per snap.

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots

parts:
  ccomixmanager:
    source: http://numerone.altervista.org/Downloads/ccomixmanager-0.1.2.6.tar.xz
    # See 'snapcraft plugins'
    build-packages:
    - libgtk-3-dev
    - libmariadb-dev
    stage-packages:
    - libmariadb3
    plugin: meson
