name: ccomixmanager # you probably want to 'snapcraft register <name>'
base: core20 # the base snap is the execution environment for this snap
version: '0.1.2' # just for humans, typically '1.2+git' or '1.3.2'
summary: Un gestionale per snap in gtk e mariadb # 79 char long summary
description: |
  Un gestionale in gtk e mariadb per snap.

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  ccomixmanager:
    source: http://numerone.altervista.org/Downloads/ccomixmanager-0.1.2.tar.xz
    # See 'snapcraft plugins'
    plugin: meson
